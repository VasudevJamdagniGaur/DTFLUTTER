/// Firebase Web API key (not committed).
///
/// Setup (pick one):
/// 1. Copy this file to `firebase_secrets.dart` in the same folder and set
///    [firebaseApiKey] to your key from Firebase Console.
/// 2. Or run with: `--dart-define=FIREBASE_API_KEY=your_key`
///
/// `firebase_secrets.dart` is listed in `.gitignore`.
const String firebaseApiKey = '';
